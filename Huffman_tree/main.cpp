#include<iostream>
#include<vector>
#include<string>
#include<unordered_map>

using namespace std;

int main()
{
	string s_in;
	bool running = true;
	while (running)
	{
		s_in.clear();
		getline(cin, s_in);
		if (s_in == "0")
		{
			running = false;
		}
		else
		{

		}
	}
	return 0;
}